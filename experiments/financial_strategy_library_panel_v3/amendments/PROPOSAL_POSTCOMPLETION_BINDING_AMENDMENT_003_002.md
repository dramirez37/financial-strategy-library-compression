# Proposal Postcompletion Binding Amendment 003 Lock 002

Lock 001 closed the artifact-denominator and result-file chain.  This additive lock
also replays the source bindings embedded in Computation Amendment Lock 003 and all
three interface locks.  When an interface source was transparently superseded by a
later interface lock, the current file must match the latest sealed digest for that
same path; otherwise it must match its original digest.  No earlier lock or artifact
is replaced.  Proposal values remain known and evaluation access remains zero.
