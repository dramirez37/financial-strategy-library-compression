% Migration IDs: P-03-001 through P-03-003 and T-03-* in the manifest.
\section{Financial strategy-library model and sufficient state}
\label{sec:model}
\label{sec:model-projection}

\DraftSectionSummary{This section will define the finite source library,
belief-indexed operating profiles, strategy modules, the declared closure
operator, additive retention burden, and the mandatory inactive strategy. It
will then state the raw-to-compressed projection under the assumptions already
recorded in the theorem ledger.}

The main semantic object will be the productive library state
\(K_L=(F_L,C_L)\), with the explicit qualification that it is the library
component of the full controlled state. Exact source-relative compression will
preserve both objects and retain the inactive strategy.

\DraftPlaceholder{Migrate the established definitions and
\texttt{thm:raw-to-compressed-projection} without changing their mathematical
meaning. Move auxiliary finite identities and proof detail to Online
Resource~1.}
