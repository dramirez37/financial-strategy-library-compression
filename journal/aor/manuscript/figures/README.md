# Figure input policy

This directory is the manuscript-facing figure input layer. Final figures must
be generated from committed artifacts, use descriptive captions and accessible
line or marker encodings, and satisfy the contrast requirements recorded in the
official journal audit.

`generated_inputs.tex` inputs only local, source-traceable TikZ assets. Those
assets are copies of committed generated figures; the historical source tree
is not modified.
