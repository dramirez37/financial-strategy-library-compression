# Fixtures

`TheoremFeasibility.lean` and `SingleGapGeometry.lean` are exact data-only
fixtures generated by the Julia falsification searches. They make no theorem
claims; the corresponding propositions and proofs live in proof modules.
