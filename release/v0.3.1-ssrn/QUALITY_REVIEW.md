# Document and evidence verification

Prepared 18 September 2026 for v0.3.1-ssrn.

The substantive review and its remaining blocker are in `READINESS_REVIEW.md`.
The release is a revised review package; an unqualified SSRN-readiness claim is
withheld. No SSRN submission has been made.

Completed verification:

- Read the current article and supplement, supporting mathematical arguments,
  model implementation, proposal-screen implementation, and evidence mappings.
- Added three editable, reproducible vector figures; independently reconciled
  their numerical inputs and all synthetic intervals with the sealed records.
- Corrected the embedded-decision projection scope, finite-horizon boundary,
  optional bridge-value bound, unit-weight normalization explanation, and DP
  predecessor argument. Expanded the formal-evidence correspondence.
- Disclosed the financial screen's reflected-error construction and unsupported
  coverage interpretation. Preserved all recorded choices, null/adverse results,
  parameters, seeds, worlds, and public evidence.
- Passed the local theorem fixture gate, full Lean build, manuscript linter,
  and axiom audit of 784 declarations (only the recorded standard Lean axioms).
- Passed the targeted Julia enumeration, DP, greedy, deletion, preprocessing,
  mixed-integer, and exactness-audit test suite.
- Passed the read-only benchmark audit, matching committed certificates,
  manifests, and reports byte for byte: 173 instances, 3,907 terminal units,
  3,526 accepted libraries, 381 unsuccessful units, 31/38 complete agreements.
- Replayed the calibrated synthetic study from its public calibration and fixed
  seeds; all 16,384 world rows match (`V4_RESULT_AUDIT_OK`).
- Verified all 31 public evidence hashes and all 136 selected scientific-source
  hashes against the pinned repository revision. No scientific source changed.
- Rebuilt article, supplement, and combined PDF; checked searchable text,
  embedded scalable fonts, non-encryption, resolved references, and preserved
  internal destinations. Rendered pages and enlarged figures/tables were inspected.
- Extracted the editable archive into an empty directory and rebuilt it, including
  figure-data checks and independent review fixtures. Compared text, page counts,
  and internal-link structure with the packaged PDFs.
- Audited the staged public tree and release package. No raw licensed observations,
  private identities, local caches, or unrelated active research are included.

`PDF_AUDIT.json`, `EVIDENCE_CHECK.json`, `REVIEW_CHECK.json`, source manifests,
and SHA-256 checksums provide the machine-readable record. The substantive
review identifies the unresolved repository-wide source-lock failure; no full
regression success is claimed. Font and audit-tool omissions in CI dependencies
were repaired without weakening scientific checks.

The licensed financial stages were not rerun. The original benchmark was not
regenerated. The separate Genealogies research and its failed pilot were not
modified or executed.
