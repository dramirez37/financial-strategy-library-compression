% This file is intentionally an input layer. No scientific result is drawn or
% transcribed here. Final macros must use \input or \includegraphics on a
% generated, source-traceable artifact listed in the migration manifest.
\newcommand{\AOREconomicGeometryFigure}{%
  \begin{figure}[tbp]
    \centering
    \resizebox{\textwidth}{!}{%
      \input{figures/unified_economic_geometry}%
    }
    \caption{Exact finite illustrations of hard-capacity and resource-price
    geometry. Panel (a) shows right-continuous capacity steps; panel (b) the
    upper envelope of affine net-value branches; panel (c) the associated
    optimizer burden under the declared tie rule. Solid and dashed lines are
    distinguishable without color. Coordinates are generated from committed
    exact rows and are illustrative fixtures, not statistical estimates.}
    \label{fig:canonical-economic-geometry}
  \end{figure}}
\newcommand{\AORFinancialComparisonFigure}{%
  \begin{figure}[tbp]
    \centering
    \resizebox{\textwidth}{!}{%
      \input{figures/financial_innovation_safe_compression}%
    }
    \caption{Retrospective financial safety--compression comparison for the
    historical stepwise endpoints. Panels (a)--(b) report retained shares of
    each fixed source library and its module closure. Panel (c) reports the
    postdecision change in the ex post catalog-maximum witness; the terminal
    and annual audit units are separate and are not pooled. Both rules preserve
    the validation frontier, but only innovation-safe retention preserves
    closure. Equality of the witness for safe libraries follows mechanically
    from closure equality, and the maximum is not a multiple-testing-adjusted
    performance estimate. Direct row labels and distinct hatch patterns make
    the comparison readable without color. Displayed diagnostics were never
    pruning inputs.}
    \label{fig:financial-safe-compression}
  \end{figure}}
