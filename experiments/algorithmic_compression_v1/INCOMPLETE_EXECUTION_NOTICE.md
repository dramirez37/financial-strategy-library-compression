# Registered Algorithmic Compression Benchmark v1 — incomplete execution notice

The v1 final execution was stopped at the user's request on 2026-08-27 after
1,634 of 3,907 registered run records had become terminal and 68 of 173 final
instances had been generated. No result-level burden, runtime, solver, or
algorithm comparison was used to change a v1 row, seed, limit, algorithm, or
analysis rule. One in-flight process was interrupted; it had not written a
worker result.

The partial v1 artifacts are not a complete registered benchmark and must not be
presented as its final evidence. They are not pooled with Registered Algorithmic
Compression Benchmark v2. The v1 locks, registries, seeds, design, and partial
raw artifacts remain historical records and are not rewritten.

V2 is a separately registered eight-worker study with new identifiers, seeds,
configuration, result root, and design lock. It preserves single-threaded
algorithm and solver semantics while parallelizing independent run units at the
supervisor level.
